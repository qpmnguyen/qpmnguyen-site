---
title: "Bayesian approach to estimating means with PyMC3"
description: |
  A short description of the post.
author:
  - name: Quang Nguyen
    url: {}
date: 2022-01-23
output:
  distill::distill_article:
    self_contained: false
    code_folding: true
    highlight: pygments
    highlight_downlit: false
draft: true
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```


Setting up the `Julia` environment  

```{r, echo=TRUE, results='hide', cache=TRUE}
library(JuliaCall)
JuliaCall::julia_markdown_setup()
pkgs <- c("Gadfly", "DataFrames", "DataFramesMeta", "CSV", "Chain", "CategoricalArrays", "Turing", "Random", "Statistics", "Distributions", "MCMCChains", "Optim")
sapply(pkgs, julia_install_package_if_needed)
```


First, let's import all our packages:  

Load `python` packages  

```{julia}
using Distributions
```


```{r}
library(tidyverse)
```

Load our `R` packages  

```{r}
data <- read_csv(file = "https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2022/2022-01-18/chocolate.csv")
head(data)
```




